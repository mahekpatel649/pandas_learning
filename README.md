# pandas_learning
My Pandas learning and practice repository, containing basic concepts, data manipulation, indexing, filtering, cleaning, and hands-on exercises using Python Pandas.
# Pandas Learning 🐼

This repository contains my hands-on learning and practice with **Python Pandas**.

I worked with an employee dataset and practiced different techniques for exploring, cleaning, transforming, and analyzing tabular data.

## Topics Covered

- Installing and importing Pandas
- Series and DataFrames
- Loading CSV data
- Data inspection with `head()`, `tail()`, `sample()`, `shape()`, `dtypes`, `info()`, and `describe()`
- Selecting and filtering data
- Indexing with `loc` and `iloc`
- Handling missing values
- Removing and filling null values
- Converting data types
- Detecting and removing duplicate data
- Converting text to datetime
- Renaming and modifying columns
- Creating new columns
- Using `apply()` and `map()`
- Sorting and ranking data
- Finding unique values
- `nlargest()` and `nsmallest()`
- Grouping data with `groupby()`
- Aggregation with `mean()`, `sum()`, `max()`, `count()`, etc.
- Multiple-column grouping and aggregation

## Tools Used

- Python
- Pandas
- Google Colab
- Jupyter Notebook

## Dataset

The practice in this notebook uses an employee dataset to apply Pandas concepts to practical data analysis tasks.

## Purpose

This notebook is part of my **Data Science learning journey**, where I am building my foundation in Python-based data analysis through hands-on practice.
